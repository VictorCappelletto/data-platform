workflow_id: orders_demo
description: Local end-to-end orders chain (ingest → KPI → export)
orchestrator: workflows.runs.orders_demo:run

tasks:
  orders_pipeline:
    orchestrator: workflows.runs.orders_demo:run
    domain_module: ingestion.orders:run_pipeline
  kpi_metrics:
    orchestrator: orchestrator.analytics.kpi_metrics:run
    domain_module: transformation.kpi:run_kpi_pipeline
  analytics_export:
    orchestrator: orchestrator.analytics.analytics_export:run
    domain_module: transformation.export:run_export
