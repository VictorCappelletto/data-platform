workflow_id: brewery_demo
description: Local end-to-end brewery chain (ingest → DQ → gold)
orchestrator: workflows.runs.brewery_demo:run

tasks:
  full_pipeline:
    orchestrator: workflows.runs.brewery_demo:run
    domain_module: transformation.brewery:run_full_pipeline
