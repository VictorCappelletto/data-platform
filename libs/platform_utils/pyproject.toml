[project]
name = "platform-utils"
version = "0.1.0"
description = "Shared logging, retry, and date helpers for the data platform"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
